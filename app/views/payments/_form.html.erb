<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contract_id %><br>
    <%= f.collection_select :contract_id, Contract.where(landlord_id: current_landlord.id), :id, :description %>
  </div>
  <div class="field">
    <%= f.label :full %><br>
    <%= f.check_box :full %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
